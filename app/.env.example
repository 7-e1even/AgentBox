# URL of the independent Go API. Database credentials belong in server/.env.
AGENTBOX_API_URL=http://127.0.0.1:8091
# HTTPS public entry used by browsers and physical servers running AgentBox Worker.
# Plain HTTP is accepted only on exact localhost/loopback addresses for same-host development.
AGENTBOX_PUBLIC_URL=https://agentbox.example.com
