# Next.js agent rules

This project uses a recent Next.js release with breaking API and convention changes. Before modifying framework behavior, read the relevant guide under `node_modules/next/dist/docs/` and follow current deprecation notices.
